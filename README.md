# RANKUP — 두뇌 랭크전

**▶ https://17vs01.github.io/rankup/**

심심할 때 쇼츠 대신 여는 앱. 열면 바로 할 게 있고, **한 판 60초**, 끄고 나면 실력이 숫자로 남는다.

폰 브라우저로 위 주소를 열고 **홈 화면에 추가**하면 앱처럼 쓸 수 있다. 오프라인에서도 돌아간다.

## 왜 이렇게 만들었나

쇼츠/틱톡이 이기는 이유는 재미가 아니라 **마찰이 0**이기 때문이다. 그래서 같은 조건을 맞췄다.

| 조건 | 쇼츠 | RANKUP |
|---|---|---|
| 열자마자 할 게 있나 | O | O (선택 화면 없음, 빠른 시작 1탭) |
| 30초 세션 성립 | O | O (60초 한 판) |
| 콘텐츠 고갈 | 없음 | 없음 (문제 절차 생성) |
| 끄고 나서 남는 것 | **없음** | **랭크 + 실제 실력** |

의지력으로 쇼츠를 참는 게 아니라, 같은 습관 자리를 뺏는 쪽으로 설계했다.

## 종목 9개

| 종목 | 내용 | 실생활 |
|---|---|---|
| 📚 **어휘력** | 우리말 1300+ · 영단어 600+ — 골라서 또는 통합(랜덤 혼합) | 글쓰기·독해 |
| 🧩 **24 만들기** | 숫자 4개로 목표값 만들기 · 타임어택 | 수 감각·역산 |
| 🧮 **암산** | 60초 연산 스프린트 | 장보기·팁·환율 암산 |
| 🧠 **기억력** | 순간 패턴 기억 6라운드 (적응형, 최고 칸수 기록) | 작업기억 |
| ⚡ **집중력** | 반응속도·스트룹·고/노고 골라서 단련 + 레벨 | 주의 전환·충동 억제 |
| 🎭 **예측 불가** | AI가 내 다음 수를 예측. 적중률 50%면 만점 | 가위바위보·협상·포커 |
| ⏳ **체내 시계** | 화면 없이 목표 시간 맞히기 | 요리·발표 시간·인터벌 운동 |
| 🧭 **나침반** | 지시를 머릿속으로 따라가고 출발점 방향 답하기 | 지하철 출구·주차장에서 차 찾기 |
| 📐 **눈대중** | 각도·비율·개수·넓이 순간 추정 | 가구 배치·수량 판단 |

### 🎭 예측 불가 — 이 앱의 핵심

1953년 벨연구소에서 클로드 섀넌이 만든 "마인드 리딩 머신"을 폰으로 옮긴 것. 사람은 무작위를 못 만든다 — "아무렇게나" 누른다고 믿어도 반드시 버릇이 남는다.

좌/우 버튼 두 개로 60번. 매번 AI가 **먼저** 예측하고, 누른 뒤에 맞았는지 공개된다.

- AI는 다중 차수 마르코프 예측기다. 랭크가 오르면 기억하는 문맥이 2수 → 8수로 늘고, 1400 LP부터는 **누르는 속도(리듬)**까지 문맥에 넣는다.
- 적중률 44%까지 떨어뜨리면 본전, 50%(완전 무작위)면 상승.

봇으로 검증한 결과:

| 플레이 방식 | AI 적중률 | LP |
|---|---|---|
| 인간형(70% 교대 편향) | 58% | −1 |
| 진짜 무작위 | 47% | **+7** |

사람이 "무작위"라고 착각하는 패턴은 실제로 읽힌다.

## 매일 여는 이유

두뇌 훈련 앱이 이기는 방식은 "재미"가 아니라 **매일 열 이유**다. 그래서 네 가지를 붙였다.

| 장치 | 하는 일 |
|---|---|
| 🎯 **오늘의 훈련** | 매일 3종목을 골라준다 (부식 임박 → 미플레이 → 평균 미달 순). 목록 11개 앞에서 고민하지 않게 한다 |
| 🗓 **오늘의 도전** | 날짜를 시드로 써서 **전 유저가 같은 문제**를 푼다. 결과는 한 덩어리 텍스트로 공유 |
| 🏆 **주간 리그** | 토스 리더보드에 평생 누적이 아니라 **이번 주에 딴 LP**를 올린다. 월요일 새벽 4시 리셋 |
| ❄ **스트릭 보호권** | 7일 연속마다 1개(최대 2개). 하루 빠지면 자동으로 하나 쓰고 연속을 지킨다 |

평생 누적을 올리면 상위권이 고착돼 새로 온 사람이 포기한다. 주간 리셋은 매주 모두를 같은 출발선에 세운다.

### 🔢 스도쿠 — 랭크 밖 별관

스도쿠는 **랭크 종목이 아니다.** 한 판이 5~20분인데 나머지는 60초라, 같은 LP 경제에 두면 분당 LP가 12배 차이 난다 — 주간 리그를 도입한 순간 "하면 손해인 종목"이 됐다. 그래서 LP·부식·종합 점수·리그·오늘의 훈련에서 전부 빼고 홈 아래 **별관**으로 옮겼다.

대신 자체 진행 장치를 준다.

- **난이도 해금**: 쉬움을 완성해야 보통이 열린다 (쉬움 42단서 → 극한 25단서, 6단계)
- **난이도별 최단 기록**: 쉬움 기록과 극한 기록은 별개
- **이어하기**: 중단해도 판이 남는다. 다른 난이도를 고르면 진행 중인 판이 사라지므로 먼저 확인받는다
- 스트릭에는 인정된다 — "오늘 앱을 열고 뭔가 했다"의 지표라서

## 랭크 시스템

**브론즈 → 실버 → 골드 → 플래티넘 → 다이아 → 마스터 → 그랜드마스터** (1000 LP 시작)

- **ELO 방식**: 절대 점수가 아니라 *현재 레이팅의 기대치 대비* 성과로 LP가 오르내린다. 한 판 최대 **+40 / -24**.
- **난이도 자동 추적**: 레이팅이 오르면 문제도 어려워진다. 암산은 `3+5`에서 시작해 `47×63`, `38²`까지 간다. 그래서 정답 개수는 실력이 늘어도 비슷하게 유지되고, **오르는 건 랭크뿐**이다.
- **어휘 난이도**: 레이팅 400마다 단어 티어가 오른다 (기초 → 수능 → 토익 고득점 → GRE급).
- **기억력 적응형**: 성공하면 +1칸, 실패하면 -1칸. 매 판 자기 한계선에서 논다.
- **눈대중·체내 시계**: 레벨이 오르면 허용 오차가 조여든다 (±14% → ±2.5%). 체내 시계는 2단계부터 **엉뚱한 속도로 차오르는 가짜 진행바**가 붙어 체감을 흔든다.
- **나침반**: 지시가 3단계에서 12단계로 늘고 표시 속도가 빨라진다. 정답 8방위가 균등하게 나오도록 경로를 역으로 뽑기 때문에 "그냥 뒤 찍기"가 통하지 않는다.

### 패널티 — 방치 부식

**48시간** 안 하면 하루 **-8 LP**씩 깎인다. 단, **최고점 -250**에서 멈춘다 (며칠 쉬었다고 바닥까지 떨어지진 않음).

홈 화면에 부식까지 남은 시간이 표시되고, 깎였으면 경고 배너가 뜬다. 이게 "오늘 한 판" 하게 만드는 장치다.

### 라이트너 복습 (어휘)

틀린 단어는 다음 판에 바로 다시 나온다. 맞히면 1일 → 2일 → 3일... 간격으로 밀린다. 반복만 해도 어휘가 실제로 쌓인다.

## 폰에서 쓰기

### 방법 A — 맛보기 (같은 와이파이, 5초)

PC에서 서버를 켜고 폰으로 접속한다.

```bash
npx -y http-server C:\projects2\enjoy -p 8734 -c-1
```

`ipconfig`로 `IPv4 주소`를 확인해서 폰 브라우저에 `http://<그 IP>:8734` 입력.

> ⚠️ **이 방식은 오프라인이 안 된다.** 서비스워커는 HTTPS 또는 localhost에서만 등록되는데
> `http://192.168.x.x`는 둘 다 아니다. 그래서 PC 서버가 꺼지면 앱도 안 열리고,
> Android Chrome의 "앱 설치"도 안 뜬다. 게임과 랭크 기록 자체는 정상 동작한다.
> 포트가 이미 쓰이는 중이면 `EADDRINUSE` 에러가 난다 — 서버가 이미 떠 있다는 뜻이니 그냥 접속하면 된다.

### 방법 B — 배포본 쓰기 (권장)

이미 GitHub Pages에 올라가 있다. 폰에서 그냥 열면 된다.

**https://17vs01.github.io/rankup/**

고친 뒤 다시 올리려면 푸시만 하면 된다 (빌드 과정 없음):

```bash
git push
```

> 배포 후 캐시된 옛 버전이 보이면 `sw.js`의 `CACHE` 값을 올려서 푸시한다.
> 서비스워커는 네트워크 우선이라 보통은 그냥 갱신된다.

폰에서 열고 **홈 화면에 추가**:
- iPhone(Safari): 공유 버튼 → `홈 화면에 추가`
- Android(Chrome): ⋮ → `앱 설치`

이제 서비스워커가 전부 캐싱해서 **비행기 모드에서도** 돌아간다. 기록은 폰 localStorage에 남는다.

## 시간 기록

**해결 시간이나 도달 수준이 실력을 뜻하는 종목에만** 넣었다. 60초 고정 스프린트(암산·어휘)는 시간이 항상 같아서 기록이 의미가 없다.

| 종목 | 기록하는 것 |
|---|---|
| 🧩 24 만들기 | 한 문제 최단 · 5문제 전체 최단 · 타임어택 최고 단계 |
| ⚡ 집중력 | 최고 반응속도 (ms, 부정출발 제외) · 3종목 레벨 |
| 🧠 기억력 | 최고 칸수 |
| 🎭 예측 불가 | 최저 AI 적중률 · 최다 연속 회피 |

기록은 결과 화면에 `⏱ 쉬움 완주 신기록 — 3:42 (이전 5:10)` 처럼 이전 기록과 함께 뜨고, 홈 목록과 게임 방법 화면에서도 볼 수 있다. 백업 파일에 함께 저장된다.

## 게임 방법

종목마다 방법 화면이 있다 — 하는 법(단계별) / 점수와 랭크 / 요령, 일부는 배경 설명까지.

- 홈 목록의 **?** 버튼으로 언제든 열 수 있다
- **처음 하는 종목은 자동으로** 방법부터 보여준다 (한 번 보면 다음부터 바로 시작)
- 시작 카운트다운 3초 동안 한 줄 요약을 다시 보여준다

내용은 `js/rules.js` 한 파일에 모여 있다.

## 화면 테마

설정에서 셋 중 고른다. 색은 전부 WCAG AA(4.5:1)를 넘긴 값만 쓴다 — 흐린 글자를 더 흐리게 만들면 "고급스럽다"가 아니라 "안 보인다"가 된다.

| 테마 | 배경 | 최저 명암비 |
|---|---|---|
| **오닉스** 깊은 검정 | `#0A0A0C` | 4.68:1 |
| **그래파이트** 차분한 회색 | `#1D1E21` | 4.76:1 |
| **리넨** 밝은 아이보리 | `#F6F4F0` | 4.89:1 |

## 기록 백업

기록은 브라우저 localStorage에만 있다. 브라우저 데이터를 지우면 랭크가 날아가므로 **설정 → 파일로 내보내기**로 가끔 저장해 둔다. 파일 하나면 다른 기기에서도 그대로 되살아난다.

가져오기는 덮어쓰기 전에 **파일 내용을 먼저 보여주고 확인받는다** (종목 수, 누적 판수, 마지막 플레이, 내보낸 시각). 형식이 아닌 파일은 이유를 밝히며 거부한다 — `JSON 형식이 아닙니다` / `RANKUP 백업 파일이 아닙니다` / `'math' 기록이 손상되었습니다`.

## 구조

```
index.html              화면 7개 (첫안내 / 홈 / 게임 / 결과 / 방법 / 기록 / 설정)
css/style.css           근사 블랙 + 골드 단일 강조, 세로 모바일 전용
sw.js                   오프라인 캐시
manifest.webmanifest    PWA 설치 정보
js/
  main.js               화면 전환, 세션 수명주기, 결과 처리
  platform.js           앱인토스 어댑터 (별명·리더보드·뒤로가기, TOSS.md 참고)
  daily.js              날짜 시드 난수 — 오늘의 도전(전 유저 동일 문제)
  feedback.js           판정 플래시 (◯ / ✕)
  rating.js             티어·LP 계산, 부식 공식
  storage.js            localStorage 저장, 스트릭, 세션 기록
  audio.js              사운드 + 진동 피드백
  backup.js             기록 내보내기·가져오기 (형식 검증 포함)
  sudoku-core.js        스도쿠 생성기·솔버 (유일해 보장, 1~3ms/판)
  data/vocab.js         어휘 600+ (티어 0~3)
  data/korvocab.js      한국어 어휘 1300+ (전용 오답·예문 포함)
  games/                math · lexi(어휘력) · memory · focus · unpredict
                        chrono · compass · eyeball · t24
                        sudoku (랭크 밖 별관 — GAMES 배열에 없다)
```

빌드 도구 없음. 의존성 없음. 순수 ES 모듈.

### 게임 추가하는 법

`js/games/`에 아래 형태로 만들고 `main.js`의 `GAMES` 배열에 넣으면 끝. 랭크·부식·기록은 자동으로 붙는다.

```js
export const myGame = {
  id: 'mygame', name: '이름', icon: '🎯', desc: '한 줄 설명',
  run(ctx) {
    // ctx.rating    현재 레이팅 → 난이도 조절에 사용
    // ctx.body      게임 DOM 컨테이너
    // ctx.rng()     난수. 오늘의 도전이면 시드 난수가 들어와 모두 같은 판이 된다
    // ctx.daily     지금이 오늘의 도전인가
    // ctx.delay()   세션 종료 시 자동 정리되는 setTimeout
    // ctx.timer(초, 콜백)   상단 카운트다운
    // ctx.stopwatch()       흐르는 시간 (제한 없는 종목용). 경과초를 읽는 함수를 반환
    // ctx.persist()         state 저장 (스도쿠 이어하기 등)
    // ctx.onAbort = fn      ✕ 누르기 직전 호출 — 진행 상황 저장에 쓴다
    // ctx.setTitle(문자열)
    // ctx.finish({ score, perf, detail })
    //   perf = 실제성과 / 기대성과. 1.0이면 LP 변동 0.
  },
};
```

`perf`만 제대로 잡으면 랭크가 자기평형을 이룬다. 기대치를 현재 레이팅에 비례하게 두는 것이 핵심.

시작할 때 고를 게 있는 종목(집중력·어휘력)은 `picker(state, host, onStart)`를 추가로 내보낸다.
카운트다운 **앞**(방법 화면)에서 불리므로, 게임이 시작된 뒤 흐름이 끊기지 않는다.
한 판이 긴 종목은 `long: true`를 달아 오늘의 훈련·첫 판 추천에서 빠지게 한다.
